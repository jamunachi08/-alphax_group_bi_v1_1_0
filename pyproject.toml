[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "alphax-group-bi"
version = "1.1.0"
description = "Financial matrix builder for Frappe / ERPNext"
readme = "README.md"
requires-python = ">=3.10"
